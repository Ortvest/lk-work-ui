import { Typography } from '@mui/material';

export const Text = (): JSX.Element => {
  return (
    <Typography variant="body1">
      To start using our service, please log in and explore the features we offer.
    </Typography>
  );
};
